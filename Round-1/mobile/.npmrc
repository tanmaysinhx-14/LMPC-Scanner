# Expo SDK 57 currently brings optional React/React Native peer variants that
# conflict with the app's React 19 toolchain. Keep local and EAS clean installs
# on the same resolution used to generate package-lock.json.
legacy-peer-deps=true
